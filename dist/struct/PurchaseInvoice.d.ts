import { AddAttachmentOptions, AddNoteOptions, APIPurchaseInvoice, UpdatePurchaseInvoiceOptions } from "../types";
import { Administration } from "./Administration";
import { Note } from "./Note";
import { Event } from "./Event";
import { Attachment } from "./Attachment";
import { DocumentDetail } from "./DocumentDetail";
export declare class PurchaseInvoice {
    administration: Administration;
    id: string;
    administration_id: string;
    contact_id: string;
    reference: string;
    date: Date;
    due_date: Date | null;
    entry_number: number;
    state: string;
    currency: string;
    exchange_rate: string;
    revenue_invoice: boolean;
    prices_are_incl_tax: boolean;
    origin: string;
    paid_at: Date | null;
    tax_number: string;
    total_price_excl_tax: number;
    total_price_excl_tax_base: number;
    total_price_incl_tax: number;
    total_price_incl_tax_base: number;
    created_at: Date;
    updated_at: Date;
    version: number;
    details: DocumentDetail[];
    notes: Note[];
    attachments: Attachment[];
    events: Event[];
    constructor(administration: Administration, data: APIPurchaseInvoice);
    update(options: UpdatePurchaseInvoiceOptions): Promise<this>;
    delete(refresh_journal_entries?: boolean): Promise<void>;
    addNote(options: AddNoteOptions): Promise<Note>;
    deleteNote(noteId: string): Promise<void>;
    addAttachment(options: AddAttachmentOptions): Promise<void>;
    deleteAttachment(attachmentId: string): Promise<void>;
    private setData;
}
