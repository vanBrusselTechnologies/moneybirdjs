import { AddAttachmentOptions, AddNoteOptions, APIGeneralDocument, UpdateGeneralDocumentOptions } from "../types";
import { Administration } from "./Administration";
import { Note } from "./Note";
import { Event } from "./Event";
import { Attachment } from "./Attachment";
import { Contact } from "./Contact";
export declare class GeneralDocument {
    administration: Administration;
    id: string;
    administration_id: string;
    contact_id: string | null;
    contact: Contact | null;
    reference: string;
    date: Date;
    due_date: Date | null;
    entry_number: number;
    state: string;
    exchange_rate: string;
    created_at: Date;
    updated_at: Date;
    version: number;
    notes: Note[];
    attachments: Attachment[];
    events: Event[];
    constructor(administration: Administration, data: APIGeneralDocument);
    update(options: UpdateGeneralDocumentOptions, removeContact?: boolean): Promise<this>;
    delete(refresh_journal_entries?: boolean): Promise<void>;
    addNote(options: AddNoteOptions): Promise<Note>;
    deleteNote(noteId: string): Promise<void>;
    addAttachment(options: AddAttachmentOptions): Promise<void>;
    deleteAttachment(attachmentId: string): Promise<void>;
    private setData;
}
